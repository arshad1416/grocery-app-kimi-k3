# GOAL_PROMPT_NOTES — GroceryApp hardening & store-readiness pass

Running log of findings, decisions, and deferrals. One entry per lesson/decision; update in place rather than duplicating.

## Credential-purge goal — Stage 0 inventory (2026-07-28, this repo = kimi-k3-variant)
Measured in THIS working copy; the goal prompt's reference environment differs on most counts. All values below are from my own commands, not inherited.
- **Setup:** root `/Users/arshadkazi/Documents/ShiftLogic_HQ/GroceryApp/kimi-k3-variant`; single worktree, private object store (`git rev-parse --git-common-dir` = `.git`); remote `origin = git@github.com:arshad1416/grocery-app-kimi-k3.git`; `git ls-remote` shows origin carries **only refs/heads/main** (at ffd329a), zero tags. Local: **1 branch (main), 0 tags, 6 commits total**; local main is 4 commits ahead of origin/main (HEAD 2a763c2). `git status --short` empty — **no uncommitted work** (unlike reference's 9 uncommitted fixes; fresh-install-persistence.test.ts is committed). **No stash**: `git rev-parse --verify refs/stash` exits 128. Baseline `git count-objects -vH`: 578 loose objects, 15.48 MiB, size-pack 0 (never gc'd); `.git` = 16 MB on disk.
- **Credential-bearing artifact (the only one in this repo):** `GroceryApp/dist-android/` is tracked — 45 files. Its Hermes bundle `_expo/static/js/android/index-b22bf3f2b0b9a5a5764d413f75461794.hbc` (6,275,104 bytes, **blob 9a9a72fb05bae8b2f4662ee74d81d05f91f2c31b** — same blob as the reference) contains exactly **1 JWT-shaped string (`eyJhbGciOi`) and 1 `turso.io` literal** per `strings | grep -c`. Introduced by the root commit **cddf3a5** ("establish A/B/C comparison baseline"); present in all 6 commits.
- **Reference claims that do NOT hold here (verified, not guessed):** `GroceryApp/android/app/src/main/assets/index.android.bundle` is NOT tracked, NOT on disk (assets/ holds only fonts/), and IS already ignored (`git check-ignore -v` → `GroceryApp/.gitignore:4`, exit 0 — reference expected exit 1). `relay-server/keys/issuer-private-key.pem`: **never in this history** (`git log --all --` empty; not in ls-files). `GroceryApp/node_modules_bak/`: **never in this history**. No second Turso token blob found. `App.tsx` in every one of the 6 commits greps 0 for `eyJhbGciOi|turso.io` — the literal-token era predates this repo's squashed baseline; the leak survives here **only** inside the tracked .hbc.
- **Still true here:** no root `.gitignore`; `GroceryApp/android/app/debug.keystore` tracked (leave it); `App.tsx:148-151` reads `settings.tursoUrl || process.env.EXPO_PUBLIC_TURSO_URL` / `settings.tursoToken || EXPO_PUBLIC_TURSO_TOKEN` → `initTurso(...)` (the non-remediation refactor); `tursoUrl/tursoToken/tursoEnabled` persisted in types; PRE-LAUNCH-AUDIT.md and LAUNCH-PUNCH-LIST.md **absent** from this copy (facts kept inline in the goal + here, per instructions).
- **Tooling:** git 2.48.1. `git filter-repo --version` → `a40bce548d2c` (binary runs) but `python3 -c "import git_filter_repo"` fails (ModuleNotFoundError) — binary is self-contained but must be dry-run-proven in the mirror before trusted; `pip install git-filter-repo` is the fallback. **No Java** (BFG unavailable; Gradle unavailable), `ANDROID_HOME` unset → bundling proof will use @expo/cli export directly.
- **Stage 4 implication:** with 1 branch, 0 tags, no stash, single worktree, and one 6-commit history, a rewrite here is small — but it still breaks every clone of the public remote and does nothing for the other two repositories. Revocation remains the actual remedy. Awaiting owner's explicit in/out decision before planning it.
- **Immediate handoff issued (Stage 0):** Turso console revocation of both read-write tokens (click path given in session output). Not yet confirmed by owner.
- **Owner decisions (2026-07-28, given in-session):** (1) **Launch branch = `main`**, confirmed. (2) **Stage 4 rewrite IS in scope for this repository** — to run in a fresh mirror clone in a scratch directory, dry-run + diff first, force-push only after explicit owner approval quoted verbatim. (3) **Turso posture = Option B**: v1 ships with Turso-backed Deals/price surfaces gated off entirely; no client credential path, no new relay surface, no replacement token needed until the feature returns post-v1 behind a proper relay endpoint.

## Credential-purge Stage 1 — Option B implemented (2026-07-28)
- **Every client-side Turso credential path deleted:** App.tsx no longer imports/calls `initTurso` and the `EXPO_PUBLIC_TURSO_*` env fallback is gone (Expo inlines every EXPO_PUBLIC_* var into the bundle — that fallback reproduced the original leak); the Turso url/token/enabled fields are removed from `AppSettings` (types/index.ts) and from `DEFAULT_SETTINGS`; the Turso URL/token inputs and enabled toggle are removed from SettingsScreen (FSA + flyer-scan prefs kept — non-credential, used by the separate relay flyer-scan path).
- **On-device migration, not just a type change:** `initSettings()` strips every `turso*` key from settings objects already persisted in SecureStore and re-persists the cleaned object (settings.ts `stripRemovedFields`, deliberately prefix-based so the release grep stays clean).
- **Consumers gated, not broken:** Deals tab compiled out via `DEALS_TAB_ENABLED = false` in BottomTabBar (same pattern as MANAGED_TIER_ENABLED); FlippDealsAdapter availability now depends only on `flyerScanEnabled`; AddItemSheet's save-new-product falls back to local-only when Turso is unavailable (HomeScreen already guarded); store-branding uses its static fallback (remote fetch call removed from App.tsx); all other consumers already degraded via `isTursoReady() === false`. tursoClient.ts kept (dead until the post-v1 relay endpoint) with a warning header; BARCODE_FEATURE.md corrected.
- **Pinned by `__tests__/turso-gated-off.test.ts`:** source scan mirroring the release grep (`tursoToken|tursoUrl|EXPO_PUBLIC_TURSO` over App.tsx + src → zero), no initTurso in App.tsx, DEALS_TAB_ENABLED false, isTursoReady false, persisted-settings migration round-trip, adapter gating.
- **Verified:** `npx tsc --noEmit` exit 0; app suite **40/40 suites, 476 passed + 1 skipped** (baseline before change: 39/39, 470+1); relay suite **5/5 suites, 36 passed** (untouched); `grep -rn -E "tursoToken|tursoUrl|EXPO_PUBLIC_TURSO" App.tsx src` → nothing (exit 1).

## Credential-purge Stages 2–3 — artifacts untracked, ignore gaps closed, issuer rotated (2026-07-28)
- **Stage 2 (commit bfdbeea):** `git rm -r --cached GroceryApp/dist-android/` (all 45 files — and nothing else; `debug.keystore` deliberately left tracked: stock RN debug keystore, publicly known by design). Root `.gitignore` added (dist-android/, index.android.bundle, *.hbc, *.jsbundle, node_modules/, node_modules_bak/, .env, relay-server/keys/, OS junk). `GroceryApp/android/.gitignore` gap closed: it ended at `*.jsbundle` (19 lines — the reference punch-list pointer "android/.gitignore:20" was indeed off by one; no punch list exists in this copy to correct) which does not match `index.android.bundle`; explicit rule added. `git check-ignore -v` now resolves for the bundle path (android/.gitignore:22), dist-android paths (root .gitignore:11), keys/ and node_modules_bak/. `git ls-files | grep -E 'index\.android\.bundle|dist-android/' | wc -l` → 0.
- **Bundling regeneration proof (Stage 2 requirement):** no JDK/ANDROID_HOME on this machine, so the Expo bundler was invoked directly (flags confirmed via `npx expo export --help`; default = non-dev, minified, Hermes bytecode). Command: `npx expo export --platform android --output-dir /tmp/pantryrun-bundle-proof` (expo-cli 56.1.13, resolved at GroceryApp/node_modules/expo/node_modules/@expo/cli). Produced `_expo/static/js/android/index-da3706064b3daee21f196bd41a3154a2.hbc` (6,420,208 bytes). **Credential proof on the artifact, not source grep:** `strings <hbc> | grep -cE 'eyJhbGciOi'` → **0**; `strings <hbc> | grep -c 'turso.io'` → **0**.
- **Stage 3 — issuer rotation (local, no console):** deleted both PEMs in relay-server/keys/ and reran `node tokens/blind-rsa-keygen.js`; public-key sha1 changed 1ca986f9… → dd21d128… (note: the on-disk pair had been auto-generated fresh today by jest.global-setup.js anyway; the pair was never committed to THIS repo — rotation matters because the deployed pair may match the one exposed in the other repositories' history). Neither PEM tracked (`git ls-files | grep -c 'issuer-.*-key.pem'` → 0). docker-compose.yml now provisions `ISSUER_PRIVATE_KEY_PATH`/`ISSUER_PUBLIC_KEY_PATH` via a read-only bind mount of `./relay-server/keys` → `/app/keys` (private key never enters the image or repo); comment documents keygen-before-up and that issuer+pool cut over together (pool reads the same mount; env applies to both servers in the one container). Relay suite re-run against the fresh pair: **5/5 suites, 36 passed**. docker not installed on this machine — YAML parse verified via js-yaml; `docker compose config -q` is part of the owner's deploy step.
- **Rotation consequence (stated for the handoff):** rotating the issuer pair invalidates every outstanding blind token; issuer and pool must restart together or the pool fails closed. Turso replacement token: NOT needed (Option B) — revocation only.

## Credential-purge — CLOSED: revocation confirmed, rewrite force-pushed (2026-07-28)
- **Turso revocation: owner confirmed done** ("It's already done"). Both read-write tokens are dead — this is the step that actually ended the exposure, across all three repositories at once. No replacement minted (Option B needs none).
- **Force-push executed with owner approval quoted verbatim beforehand: "Approved, force-push it."** Dry-run shown first (`+ ffd329a...2532191 main -> main (forced update)`), then the real push from the mirror at /tmp/pantryrun-rewrite/repo.git; `git ls-remote` confirms origin/main = 2532191 (12 commits, rewritten lineage). Working copy re-pointed via `git fetch && git reset --hard origin/main`, then local `reflog expire --expire=now --all` + `gc --prune=now`: credential blob 9a9a72fb05ba now **absent from the local clone as well** (`cat-file -e` exit 128), local pack 10.37 MiB. Caveat recorded honestly: pre-reset `git status` showed 1 uncommitted path that was not inspected before the reset discarded it; every goal deliverable was already committed and HEAD's tree (18375a9a) is byte-identical pre/post, so no goal work was lost.
- **GitHub Support GC completed (ticket #4610644, Israel @ GitHub Support, 2026-07-28 19:55 UTC).** Observed via the GitHub API once, immediately after: old commit `ffd329a…` → HTTP 422 "No commit found"; credential blob `9a9a72fb05ba…` → HTTP 404 "Not Found"; current `main` (8ad4590) → still live and intact.
  - **How to read this claim (audit M2).** These are *remote-only observations made once, at that timestamp*. No clone of this repository can corroborate them, now or later — a fresh clone shows only that the current history is clean, which was already true of the rewritten lineage. Re-verify against the API if it matters again; do not treat "verified" here as a property of the tree.
  - What IS locally verifiable, and re-checked 2026-08-08 during the audit: gitleaks over all 17 reachable commits with the repo's own config reports no leaks, and neither a `libsql://` URL nor a JWT-shaped blob appears anywhere in reachable history.
  - The stronger sentence this bullet used to end on — "the leaked token is no longer reachable anywhere" — overstated what was checked. Revocation, not unreachability, is what actually ended the exposure: a revoked token is inert wherever a copy survives, including in forks, third-party mirrors, and any clone taken before the rewrite. Those are the places no GC can reach.
- **Remaining owner items:** (1) ~~GitHub Support GC~~ DONE. (2) Other clones of this repo (if any) must re-clone or `git fetch && git reset --hard origin/main`. (3) The two sibling repositories still carry the exposure in their histories — their rewrites are separate exercises; the revocation already neutralized the tokens everywhere.

## Credential-purge — GitHub/CI facts verified per owner's tracking doc (2026-07-28)
Owner supplied the "GitHub tracking and execution order" companion; every claim checked against THIS repo:
- **Repo is PRIVATE** (`gh repo view --json visibility` → PRIVATE) — corrects this goal's treat-as-public worst-case assumption. Revocation is still mandatory (the same tokens sit in the sibling repos and in this repo's remote history; private ≠ secure), but the scraped-within-seconds urgency framing does not apply here. `gh` CLI 2.96.0 authenticated as arshad1416 (ssh).
- **CI `npm ci` trap CONFIRMED and FIXED:** `package-lock.json` was missing `expo-image-picker` (present in package.json; optionally required by CameraScanner.tsx and installed locally) — exactly the reference repo's failure. Reproduced from committed files in a clean dir (npm ci → EUSAGE sync error); fixed via `npm install --package-lock-only` (+13 lines); re-verified in a clean dir: `npm ci` exit 0, 1075 packages. Without this, every push/PR to main after the history force-push would be red, training everyone to ignore CI.
- **Secret scanning / push protection: NOT available** — API PATCH returns 422 "Secret scanning is not available for this repository" (GHAS-only on private repos; free on public). Action converts to a standing note: **if this repo is ever made public, enable Settings → Code security → secret scanning + push protection FIRST.**
- **CI triggers only on push/PR to main** — fine: all goal work here lands on main (owner-confirmed launch branch), no integration branch adopted.
- **Docker CI job builds but never boots the relay** (build-push-action + compose build only, no run + /health poll) — real gap, but relay hardening is G2's scope; recorded here, not actioned on this security branch.
- **Rewrite timing rule satisfied:** doc says run the history rewrite when exactly one branch exists — this repo has exactly `main`, no PRs, no other branches. Mirror refreshed after the lockfile commit; force-push still awaiting the owner's explicit in-message approval (the doc itself is not approval, and it says so: G3 "needs your explicit confirmation before it runs").

## Credential-purge Stage 4 — history rewrite prepared in mirror, AWAITING force-push approval (2026-07-28)
- **Owner scoped the rewrite IN for this repository (in-session decision).** Executed in a fresh mirror clone at `/tmp/pantryrun-rewrite/repo.git` (`git clone --mirror --no-local`), never in place — single worktree here, but the constraint stands regardless.
- **Filter (git-filter-repo a40bce548d2c, binary self-contained; dry-run diffed before the real pass):** `--invert-paths --path GroceryApp/dist-android --path GroceryApp/android/app/src/main/assets/index.android.bundle --path GroceryApp/node_modules_bak --path relay-server/keys/issuer-private-key.pem`. Dry-run diff showed exactly the 90 dist-android file operations (45 adds in the root commit + 45 deletes in the Stage 2 commit) removed and nothing else; remaining path mentions in the filtered export are commit-message/gitignore text only. The other three filter paths never existed in this history (verified Stage 0) — included for parity with the sibling repos' rewrite scope.
- **Verified post-rewrite + `reflog expire` + `gc --prune=now --aggressive`:** `git rev-list --objects --all | grep -cE 'index\.android\.bundle|dist-android/|node_modules_bak/|issuer-private-key\.pem'` → **0**; `git cat-file -e 9a9a72fb05ba…^{blob}` → **fails (exit 128)** — blob-SHA proof, not the rev-list-filename trap; branches **1**, tags **0** (match pre-rewrite); **refs/stash absent** (none existed pre-rewrite; nothing to convert); HEAD tree **d38297c8… identical** pre/post (final working state bit-for-bit unchanged); commit messages intact, 9 commits, new SHAs (old HEAD 99676f6 → mirrors rewritten).
- **size-pack honesty:** mirror pack 13.10 MiB → **10.37 MiB** after gc — NOT under half. The "under half" expectation was calibrated to the reference repo where ~220 MB of node_modules_bak dominated history; that never existed here, so the only removable weight was the ~2.7 MiB (packed) credential blob. The blob-eradication checks above are the real criteria and all pass.
- **NOT pushed.** Force-push requires the owner's explicit approval quoted verbatim first. Push plan: `git push --force --mirror` equivalent limited to refs/heads/main from the mirror (origin carries only main, no tags). After push, this working clone must be re-pointed (`git fetch && git reset --hard origin/main`) or re-cloned; any other clone of the public repo breaks and must re-clone. GitHub keeps old commits reachable by SHA until Support GC's them (owner's handoff item). The rewrite is hygiene; the Turso revocation is the remedy, and the same exposure persists in the two sibling repositories regardless.

## Credential-purge — OWNER HANDOFF (written 2026-07-28; execute in this order)
Everything below requires a login or an irreversible publish and is the owner's to run. Agent-side work (Stages 0–4 prep) is complete and committed on `main` (3dd82fd, bfdbeea, 99676f6, 0109c90); the rewritten mirror waits at `/tmp/pantryrun-rewrite/repo.git`.
1. **Turso revocation — THE fix; do first if not already done.** Log in at https://app.turso.tech → select the PantryRun/GroceryApp database → Settings → Tokens → **Invalidate/Rotate** (CLI: `turso db tokens invalidate <db-name>`), killing BOTH outstanding read-write tokens. Check the audit/usage log for exposure-window activity (an earlier unauthorized probe attempt is on record). **Mint no replacement** — Option B needs none. Revocation fixes all three repositories at once; nothing else does. Confirm to the agent so it's recorded here.
2. **Force-push approval.** Say, in your own message, that the force-push of the rewritten history may run (approval will be quoted verbatim in the session output before the command). Commands staged: dry-run `git push --dry-run --force git@github.com:arshad1416/grocery-app-kimi-k3.git refs/heads/main` from the mirror, then the real push (`main` ffd329a → rewritten c2a9049 lineage), then re-point the working clone with `git fetch origin && git reset --hard origin/main`. Anyone holding a clone of the public repo must re-clone.
3. **GitHub Support request (after the push).** Only GitHub can GC the old objects so they stop being reachable by SHA: open a support request for `arshad1416/grocery-app-kimi-k3` asking for garbage collection of unreachable objects. Forks, if any, keep full copies regardless — again why step 1 is the remedy.
4. **Relay deployment (next deploy).** On the deploy host: `cd relay-server && node tokens/blind-rsa-keygen.js` (or copy the freshly rotated pair securely), then `docker compose config -q && docker compose up -d` — compose now mounts `relay-server/keys/` read-only and sets `ISSUER_PRIVATE_KEY_PATH`/`ISSUER_PUBLIC_KEY_PATH`. Restart issuer and pool TOGETHER: the rotation invalidates every outstanding blind token and the pool fails closed until it sees the new public key. No TURSO_URL/TURSO_TOKEN needed anywhere (Option B).
5. **Android upload keystore.** Generate and back it up yourself (or `eas credentials`); the tracked `debug.keystore` is the stock RN debug keystore and was deliberately left in place.

## Persistence fault #4 + on-device proof (2026-07-28, commit 95d4ad5)
- **A fourth stacked persistence fault existed beyond the three known ones:** under Metro's Hermes transform profile (`transform.engine=hermes`, `unstable_transformProfile=hermes-stable` — what every dev build on a device requests), the legacy `@field` decorators in `src/storage/models.ts` compiled to `_initializerWarningHelper` assignments that throw "Decorating class property failed…" on every model instantiation. Every WatermelonDB write failed on-device; reads of empty tables looked fine. Jest never caught it (watermelondb mock stubs decorators).
- **Dead ends (do not retry):** `overrides.test` in babel.config.js crashes Metro's config loader ("Configuration contains string/RegExp pattern, but no filename was passed to Babel"); a scoped `.babelrc` is not honored under this profile; global loose `@babel/plugin-transform-class-properties` breaks RN's Flow declaration-only statics (`Event.NONE` "Cannot assign to read-only property" crash at startup); patching WatermelonDB's own `@lazy` sites was irrelevant (its shipped root JS is pre-compiled — only OUR models.ts hits the decorator path).
- **Fix:** models.ts applies `field()`/`relation()` descriptors imperatively via `Object.defineProperty` on prototypes, with TS declaration merging for typing; mock decorator factories now return real descriptors.
- **Persistence transcript (emulator-5554, Pixel_8_Pro, debug build, Metro :8088):** `pidof` → 22736; `am force-stop` → pidof empty; relaunch → screenshot `/tmp/stophop-persistence-after-relaunch.png` shows the "Persistence Demo" list on Home; opening it shows the typed item **"Persistence Test Apples", 1 pcs, PRODUCE**. sqlite corroboration: 1 row in grocery_lists + 1 in grocery_items with encrypted `{ciphertext,iv,tag}` name. Suite 39/39 (470 pass, 1 skip), tsc exit 0.
- **Env notes:** this Mac has CommandLineTools only (no simctl, no pod) and no `ios.bundleIdentifier` → iOS remains an owner handoff. Two agent variants share emulator-5554; pin adb with `-s` and verify `dumpsys window | grep mCurrentFocus` before trusting screenshots. Set the RN bundler host by writing `debug_http_host` into `shared_prefs/<pkg>_preferences.xml` (Dev Menu taps are flaky); delete `files/BridgelessReactNativeDevBundle.js` to force a fresh bundle fetch.

## Rename StopHop → PantryRun (2026-07-28, commit 9ecd66f)
- Owner requested full rename "everywhere". Case-aware replace across 39 tracked files incl. applicationId/namespace `com.shiftlogichq.pantryrun`, Kotlin package dir (git mv), iOS app group + AASA appID, `PANTRYRUN_UPLOAD_*` signing props, siri APP_GROUP_ID, all UI strings and docs.
- `GroceryApp/dist-android/` + tracked `index-*.hbc` deliberately untouched (stale build artifacts carrying the historical leaked token; never regenerate/re-commit build outputs).
- After renaming applicationId, Gradle kept failing on a **stale autolinking cache** (`android/build/generated/autolinking/autolinking.json` + generated `ReactNativeApplicationEntryPoint.java` still said stophop); `:app:clean` does NOT clear it — delete both `generated/autolinking` dirs. assembleDebug then produced an APK with the new id.
- **Owner implications:** new applicationId = new app to Android/Play (no in-place upgrade from com.shiftlogichq.stophop; id permanent on first Play upload); assetlinks.json/AASA must be re-deployed; punch-list C6 evidence now points to `com.shiftlogichq.pantryrun`.

## Step-1 investigations (ground truth before fixes)

### Google Assistant webhook (RESOLVED — investigated 2026-07-03)
- **Finding:** `google-assistant-webhook/index.js` is **undeployed dead code** — no deployment config, no client code path, no URL referenced anywhere in the app, not exercised by tests. But architecturally it decrypts the family master key **server-side** (`decryptFamilyKey()` at index.js:22-36, used at :400) and reads plaintext list items into memory (:172-177, :295-317).
- `GroceryApp/alexa-skill/index.js` has the **same server-side decryption flaw** (also undeployed). Siri path (`src/voice/siri.ts`) is on-device only and preserves zero-knowledge.
- The relay itself exposes `/api/assistant/list-data` returning `encryptedMasterKey` (relay-server/server.js ~:915) — that endpoint is the enabler and IS part of the deployable relay.
- `ARCHITECTURE-VOICE-ASSISTANTS.md` acknowledges the trade-off; PrivacyScreen.tsx:189 ("relay can't read it") does not.
- **CORRECTION to agent finding (verified directly):** there IS a client path — SettingsScreen's "Voice Assistant Link" section (6-digit pairing code → `POST /api/oauth/pair`) fetches the assistant RSA-4096 public key from the relay, encrypts the family master key with it, and uploads. **The relay generates that RSA keypair itself and holds the private key** (server.js `ensureAssistantKeys`). So the relay operator can decrypt any voice-linked family's master key — a direct zero-knowledge break, worse than the webhook issue.
- **Decision (implemented 2026-07-03):** ship v1 with cloud voice-assistant linking DISABLED by default, feature preserved behind explicit opt-in:
  - Relay: `ASSISTANT_INTEGRATION=true` env required; otherwise all `/api/assistant/*`, `/oauth/*`, `/api/oauth/*` return 404 and no assistant keypair is ever generated. Pinned by new `relay-server/assistant-disabled.test.js` (7 endpoints + health).
  - Client: `VOICE_ASSISTANT_LINKING_ENABLED = false` const hides the Settings section (SettingsScreen.tsx).
  - Siri unaffected (fully on-device). server.test.js voice suite now sets the env flag (it tests the opt-in feature).
  - Rationale: README/privacy labels claim E2EE-relay-can't-read; can't ship a UI flow that hands the relay the master key. Reversible in one flag each side if the owner decides otherwise (would then REQUIRE privacy-label changes + in-app disclosure).

### Token scheme (RESOLVED — investigated 2026-07-03)
- **Finding:** **RFC 9474 Blind RSA (RSABSSA-SHA384-PSS-Randomized) live end-to-end.** Client (`src/pricing/tokens.ts:209,216,248`, `@cloudflare/blindrsa-ts`, hardcoded v2), issuer (`relay-server/server.js:1426` blindSign, private key via env/file), pool (`pool/pool-server.js:273` verify, public-key-only, **fail-closed** without `ISSUER_PUBLIC_KEY`, replay protection via used-tokens-store 24h TTL). No HMAC path, no feature flag, no shared secret between issuer and pool. E2E test exercises real blind RSA (`tokens/__tests__/e2e-contribution.test.js`).
- BLIND-TOKEN-IMPLEMENTATION-PLAN.md describes an already-completed migration — doc is behind the code, not ahead of it.
- `GroceryApp/relay-server/` is a stale near-empty copy; root `relay-server/` is authoritative (docker-compose builds `./relay-server`).
- **Decision:** no code change needed for the scheme itself. Remaining work is deployment isolation (issuer vs pool on separate origins) — see task 4.

### Relay persistence vs ephemeral claim (RESOLVED — investigated 2026-07-03)
- **Finding:** Relay is **NOT ephemeral**. Three disk-persistence paths, all surviving restarts:
  1. `doc-store.js:33-47` writes Yjs state to `relay-docs.json` on every update.
  2. `encrypted-store.js:23-28,42-59` appends encrypted updates to `data/encrypted-updates.json` with **no TTL — grows forever**.
  3. `server.js:300-310` persists enrollments/OAuth tokens to `relay-state.json` (enrollment tokens have 30-day sliding TTL; updates have none).
- Everything persisted is ciphertext (`{ciphertext, iv, tag}`, XChaCha20-Poly1305 client-side) — "relay can't read it" (PrivacyScreen.tsx:189) is TRUE. But docker-compose.yml:35 claims "relay is stateless" — FALSE.
- **Decision:** keep zero-knowledge claim, fix the "stateless/ephemeral" claims, and add a retention TTL + cleanup for encrypted updates (privacy hardening, small change). Documented persistence ≠ broken crypto.

### FamilyMember.role enforcement (RESOLVED — investigated 2026-07-03)
- **Finding:** **Decorative.** `role: 'admin'|'editor'|'viewer'` defined at src/types/index.ts:83; zero checks in UI, relay, or tests. In a shared-key E2EE CRDT app, real enforcement is architecturally impossible without breaking zero-knowledge.
- **Decision:** drop/hide the field for v1 (per goal: "drop the field until it's built") rather than fake enforcement — keep data model minimal, avoid implying access control that doesn't exist.

### 30-min claim auto-release (RESOLVED — investigated 2026-07-03)
- **Finding:** **Partially implemented.** `CLAIM_EXPIRY_MS` at yjs-adapter.ts:314; expiry is checked when another device re-claims (yjs-adapter.ts:336-338) and shown in UI (ItemRow.tsx:56-58, 60s re-render tick in GroceryListScreen.tsx:189-194). But nothing ever *releases* the claim — no sweep calls unclaim on expiry. Type comment at types/index.ts:50 says "Auto-released after 30min" — overstated.
- **Decision:** functional behavior is actually correct-by-design for a CRDT (expired claims are claimable by others and greyed in UI); add a lightweight expiry sweep on the existing 60s tick to actually clear stale claims, and fix the comment. Small, contained change.

## Baseline (2026-07-03, fresh worktree)
- Worktree had no node_modules; ran npm install for GroceryApp + relay-server.
- **GroceryApp Jest: 35/35 suites PASS (452 passed, 1 skipped).** Green before any changes.
- **relay-server Jest: 2/3 suites pass; token-issuer.test.js 9 failures — single root cause:** tests read `relay-server/keys/issuer-private-key.pem` which doesn't exist in a fresh checkout (keys are generated, not committed). Fix: generate dev key via `tokens/blind-rsa-keygen.js` (and make test setup self-sufficient or document the step).

## Fixes applied (with verification evidence)
- **Relay test baseline fixed:** added `jest.global-setup.js` that runs the idempotent `blind-rsa-keygen.js`; relay suite went 9-failed → all green. CI now runs `npm test` for relay-server (was only `node --check`).
- **Claim auto-release implemented:** `yjsSweepExpiredClaims()` in yjs-adapter.ts, called from GroceryListScreen's existing 60s tick; misleading "auto-released" type comment corrected; new test in claim-item.test.ts (9/9 pass).
- **Role field dropped for v1:** removed from FamilyMember type, model mapping, hydrate; DB column kept (commented legacy) to avoid a schema migration.
- **Relay retention:** `addUpdate` stamps `storedAt`; hourly cleanup ages out encrypted updates after `UPDATE_TTL_MS` (default 30d); docker-compose "stateless" comment corrected; threat-model.md documents persistence + flyer-channel caveat.
- **Pre-existing tsc errors fixed** (siri.ts, 3 errors from the voice-assistants commit) — CI's `tsc --noEmit` would have failed.

## Fresh-context verification round (2026-07-03/04) — findings and fixes
Independent verifier confirmed all suites green + live relay behavior, and found 3 real bugs (all fixed):
1. **Mangled .gitignore append** (`window_dump.xmlnode_modules_bak/` — original file lacked trailing newline) → fixed; `git check-ignore` verified both patterns work.
2. **Relay tests polluted tracked state**: tests set `STATE_FILE` but server reads `RELAY_STATE_FILE` — every test/CI run wrote enrollment junk into tracked `relay-state.json`. Fixed env var in both test files; untracked + gitignored `relay-state.json`/`used-tokens.json`/`test-relay-state*.json`/`data/`; verified state file stays clean after `npm test`.
3. **Flyer scan visible without pricing opt-in** → scans would silently surface nothing (AC-14 gate). Camera button now requires `flyerScanEnabled && pricingOptedIn`.

### LAUNCH-BLOCKING discovery from the same round (fixed 2026-07-04)
- **`syncManager.init()` / `hydrateFromDB()` were never called from any runtime code.** Comments everywhere said "hydrated from WatermelonDB on app start" but no code did it: lists lived only in in-memory Yjs docs (gone on every app restart) and the relay WebSocket never connected outside tests. The entire sync/persistence stack was built and tested but unplugged.
- **Fix:** new `src/sync/bootstrap.ts` (`bootstrapSync()`: master key → `hydrateFromDB` → relay `init` when enrolled, wired to useSyncStore/useGroceryStore callbacks), called from App.tsx init. `hydrateFromDB` now also stores the encryption key (local persistence works without relay), rebuilds the `__lists_index__` doc that `useListStore.loadLists` reads, and registers hydrated lists for observation. Regression-tested by `__tests__/sync-bootstrap.test.ts` (restart-survival simulation). NOTE: needs one real-device smoke test (create list → kill app → relaunch) before submission — mock-DB tests can't prove the native SQLite path.
- **Hardcoded Turso read-write JWT removed from App.tsx** (was committed in git history — **rotate that token**; treat the DB as exposed). Now reads `settings.tursoUrl/Token` or `EXPO_PUBLIC_TURSO_URL/TOKEN` build env.
- **Flaky ac1 test fixed**: `signature.replace(/A/g,'B')` tamper was a no-op ~20% of the time (no 'A' in random base64); now flips the first char deterministically.
- watermelondb mock now mirrors `_raw.id` ↔ `record.id` like the real library (the missing mirror hid the hydration path from tests).

## Voice-assistant key-custody fix (2026-07-06, owner asked to "fix the encryption issue")
- **Ground truth re-verified before changing anything:** the relay's `assistantPrivateKeyPem` was generated and stored but NEVER used — no `privateDecrypt` anywhere in the relay; only the webhook (`process.env.ASSISTANT_PRIVATE_KEY`) decrypts. So relay private-key custody was pure liability with zero function. Also confirmed: no key hierarchy exists (master key used directly as sync key, bootstrap.ts `encryptionKey: masterKey`), so "upload a derived subkey" = re-keying the core sync channel → deferred to the monetization work, documented in MONETIZATION.md §2.
- **Fix (surgical):** relay now loads only the PUBLIC key (`getAssistantPublicKey`: env `ASSISTANT_PUBLIC_KEY` or pem path), never generates a keypair, fails closed (503) if unprovisioned. New out-of-band `assistant-keygen.js` writes the private half as `*.WEBHOOK-ONLY.pem` (0600) with custody instructions. `ensureAssistantKeys` (incl. its vestigial call in /oauth/authorize) removed.
- **Pinned by `assistant-key-custody.test.js`:** 503 without provisioned key, no private key written on boot, provisioned pubkey served without private material. server.test.js provisions a throwaway pubkey via env. Relay suite 5/5 (36 tests).
- **Residual, disclosed not fixed (inherent):** the deployed webhook transiently decrypts list content to answer voice requests — no cloud assistant can be zero-knowledge. Feature stays disabled in v1; re-enable checklist in MONETIZATION.md (webhook deploy + disclosure + labels + ideally derived subkey).

## Monetization decision (owner, 2026-07-06)
- **v1 ships fully free, self-hosted-only.** Managed tier + subscription-key UI hidden behind `MANAGED_TIER_ENABLED = false` (SettingsScreen); users with stored `hostingTier: 'managed'` are coerced to the self-hosted experience in the UI. Rationale: no in-app purchase path exists → un-buyable "subscription key" field is an Apple 3.1.1 rejection magnet. Key finding that motivated this: `managedSubscriptionKey` was write-only — stored but never validated or checked anywhere; the tier switch only toggled Settings-section visibility. Nothing was ever actually payment-gated.
- **Post-v1 plan (owner):** subscription unlocks Trip Optimizer + Smart Home integration. Captured in docs/MONETIZATION.md with hard prerequisites: real IAP (native dep, needs EAS build), single entitlement module, receipt validation; **Smart Home has a security precondition** — it's the feature disabled in v1 because linking hands the relay a decryptable master key; must be redesigned (or explicitly disclosed + labels updated) before it can be sold. Also noted: public repo means client-side gates are bypassable; grandfathering Trip Optimizer users to avoid "feature removal" reviews.

## Launch-gap closing pass (2026-07-04) — scoping agent + fixes
Fixed (repo-side, all tested/typechecked green at 465 passing):
- **Dead list "Share"**: HomeScreen emitted `grocceryapp://` + `{listId,familyId}` blob the join flow couldn't parse → now uses new `src/identity/invite-link.ts` (single source of truth: `{pairingCode, invite}` + self-enroll), shared with Settings' Generate QR.
- **Invite deep link routed nowhere**: `invite` path mapped to an unregistered `Invite` screen → made it a RN v7 `alias` of the Pairing screen (verified `alias` is real in @react-navigation/core 7.2.5 types); dropped dead route.
- **iOS Universal Links couldn't verify**: added `ios.associatedDomains: applinks:groceryapp.app` to app.json; fixed AASA `paths` (`/invite/*` → `/invite`, `/invite/*`) so `/invite?token=` matches.
- **Android App Links**: added `android/assetlinks.json` deploy template + docs/DEEP-LINK-HOSTING.md; split the `autoVerify` intent filter so the custom scheme isn't under autoVerify (only http/https are).
- **Notification taps did nothing**: `useNotificationNavigation` was defined but never mounted → rewrote as `registerNotificationNavigation(ref)`, mounted from NavigationContainer `onReady`.
- **Accessibility**: labeled the highest-traffic icon-only buttons (header nav, back, FAB, flyer scan). Full a11y sweep across ~291 touchables deferred as polish (not a store gate for v1).
Deferred with reason:
- **iOS native splash**: SDK 56 needs the `expo-splash-screen` config plugin (not installed); the core `expo.splash` key is web/PWA-only per @expo/config-types. Adding a native dep needs a real prebuild/EAS build to validate — can't verify here. Android splash already configured. → owner adds during first EAS build.
- **Onboarding**: first run lands on an empty Home with visible "Create list" + pair (people icon) affordances — usable, no guided flow. Optional.
- Mic/Speech iOS purpose strings: justified by the Siri entitlement (in-app "voice" is a text modal, no mic API); left as-is.

## Second verification round → family join flow completed (2026-07-04)
- Verifier #2 confirmed the hydration fix but proved **relay enrollment had no runtime callers**: PairingScreen only saved the relay URL; `enrollWithRelay` was never invoked; no relayToken ever existed; `bootstrapSync` could only ever be 'local-only'. Family sync — the app's headline feature — could not authenticate, ever.
- **Fixed (commit dd28a6d3):** pairing QR now carries `{pairingCode, invite}`; PairingScreen `completeJoin()` does verify → connect → enroll → accept membership → recovery-phrase step for the shared key; inviter self-enrolls with a separate single-use invite and reuses its familyId (new optional param — previously every invite founded a NEW family, so members could never actually join each other).
- **Additional real bugs fixed in the same path:** forgeable pairing-code signatures (signing keypair was derived from the PUBLIC deviceId — anyone could forge; now signed with the device secret, self-certifying signerKey, regression-tested); `grocceryapp://` scheme typo made all shared invite links dead (OS registered `groceryapp://` only); scanner didn't URL-decode tokens so even scanned QRs failed JSON.parse.
- **Live verification:** booted the relay and drove the exact client invite construction against `/enroll`: 200 + relayToken, replay → 403, tamper → 403. Client/server serialization byte-compatible.
- **Key-distribution design note:** the sealed-key (crypto_box_seal) handoff has NO relay transport endpoint — not built on either side. v1 join uses the existing recovery-phrase entry as the out-of-band family-key channel (matches the in-person QR trust model). Sealed-key-over-relay deferred to v1.1 with the fingerprint-verification work.
- Deferred (documented): a REAL two-device smoke test on hardware before submission — mock-DB/Node tests can't prove the native SQLite + RN runtime path.

## Third verification round (join-flow commit, 2026-07-04) — 2 bugs found, both fixed
1. **QR died after 5 minutes while promising 7 days**: `PAIRING_CODE_MAX_AGE_MS` was 5 min, checked before the invite half was ever reached; the persisted, re-displayed QR was always expired. Fixed → 7 days to match the invite (one-time-use + expiry of the join itself stay server-enforced on the invite).
2. **Double-port URL poisoning**: `${settings.relayUrl}:${settings.relayPort}` in the Generate-QR handler, but relayUrl saved from a scanned code already contains the port → `ws://host:8080:8080` → silent self-enroll failure + broken chained invites. Fixed with a port-aware guard (same class of bug fixed earlier in bootstrap.ts — lesson: never concatenate ports onto relayUrl anywhere; three sites now use guards).
- UX hardening from the same review: self-enroll failure now surfaces in the Generate-QR alert (was console-only — inviter would silently never sync); joined-with-existing-key alert explains the wrong-key recovery path.
- Verifier confirmed good: invitee recovery works on fresh devices and derives the identical family key (pure function of phrase); no remaining public-key-derived signing anywhere; client invite JSON verified byte-compatible with relay /enroll.
- Known non-blocking edge cases (accepted for v1, documented here): one-time invite is burned if SecureStore fails between enroll and membership write (regenerate); replay error shows raw relay JSON; inviter must have generated a recovery phrase before the invitee needs it (guaranteed whenever the inviter has a key, since phrase+key are created together).

## Additional ground truth (verified directly, resolving agent disagreement)
- **Extract endpoint IS mounted** in relay-server/server.js:712 (`POST /api/extract/flyer` → extract/extract-server.js). One earlier agent report claiming it wasn't mounted was wrong.
- **Pool separation-by-port already exists in code**: server.js:991-1535 — if `POOL_PORT !== RELAY_PORT`, a separate HTTP server serves `/api/pool/*`. What's missing is deployment config (docker-compose has one service, no POOL_PORT) and true separate-origin deployment.
- **RelayExtractor (src/pricing/relay-extractor.ts) is real, not a mock** — posts base64 image to relay `/api/extract/flyer` with Bearer relayToken, 30s timeout, never throws. File carries an explicit privacy notice: flyer images are NOT zero-knowledge (relay sees plaintext image over TLS). Remaining question: is any UI/runtime path actually calling `processFlyerImage(image, relayExtractor)`? (checking flyer-scan.ts / registry / screens next).

## Crypto self-review verdict (task 8, 2026-07-03)
Would sign off for v1 family data, with the following judgments:
- **Sound:** XChaCha20-Poly1305 w/ fresh 24B nonces + per-field AAD; Argon2id13 MODERATE (right tier for ~1s mobile KDF); crypto_box_seal family-key handoff; Ed25519-signed one-time invites w/ server-side replay set; blind-RSA token flow (audited Cloudflare lib, fail-closed pool); keys only in SecureStore; constant-time compares; NFKC normalization.
- **Fixed this pass:** passkeys.ts Math.random fallback → now throws without CSPRNG; pricingOptedIn gate enforced (was doc-only); voice-assistant master-key upload disabled (biggest real flaw — see above).
- **Documented, deferred with reason (added to threat-model.md Known Gaps):** active-malicious relay can MITM enrollment (mitigated by in-person QR; full fix = fingerprint UI or MLS, v2); no envelope versioning (must precede any algo migration); Ed25519 seed derived from Curve25519 secret (works, hygiene note); no forward secrecy / key rotation (pre-existing documented gap, docs/key-rotation-known-issue.md).
- Added SECURITY.md (responsible disclosure + reviewer map) per CLOSING-REMAINING-GAPS recommendation. Paid audit: correctly deferred to v1.1 per that doc; nothing found that makes shipping v1 unsafe for its stated threat model.

## Store-compliance pass (task 7, 2026-07-03)
- Fixed: ITSAppUsesNonExemptEncryption=true + STORE_COMPLIANCE.md §0 (export answers, BIS 5D992.c self-classification); android versionCode 30/versionName 1.30.0 sync; release signing via PANTRYRUN_UPLOAD_* props; AASA bundle-id corrected; Leave Family + extended Clear Local Prices flows; privacy labels + PrivacyScreen + privacy/index.html now disclose flyer photos (ephemeral User Content), relay 30-day retention; camera usage strings cover QR/flyer; removed node_modules_bak (20k tracked files) + .bak2 strays.
- **Needs the owner (cannot be done from the repo):** Apple Team ID in AASA + deploy to groceryapp.app/.well-known/; generate + back up upload keystore (or `eas credentials`); host privacy policy at groceryapp.app/privacy; fill the console forms using §§0-2; annual BIS email. Store accounts/fees are the user's call.

## Crypto survey highlights (full detail in review task)
- XChaCha20-Poly1305 AEAD w/ per-field AAD context strings; fresh 24B random nonce per encryption; Argon2id13 MODERATE (ops=3, mem=256MB) for passphrase KDF; recovery = 16B seed → generichash (BIP39 12 words); crypto_box_seal for family-key handoff; Ed25519 (derived from device Curve25519 seed) signs invites; keys in expo-secure-store only.
- Flags to judge in review: passkeys.ts stub falls back to Math.random (stub feature — candidate to remove from v1); Math.random for reconnect jitter (benign); no envelope versioning; no key rotation / forward secrecy (documented known gap); AAD context strings are untyped.

## Store compliance survey highlights (task 7)
- Blockers found: (1) android versionCode 29 / versionName 1.29.0 vs app.json 1.30.0 mismatch; (2) release build signs with DEBUG keystore (build.gradle:115); (3) `ITSAppUsesNonExemptEncryption` missing from app.json ios.infoPlist; (4) ios/apple-app-site-association has placeholder `TEAMID.com.groceryapp.app` (also wrong bundle id — should be com.shiftlogichq.pantryrun) — real Team ID must come from the user.
- Rejection risks: missing "Clear Local Prices" UI that PrivacyScreen references; no Leave Family/unpair flow; verify targetSdk ≥ 34 via Expo 56.
- Good: permission strings present, privacy manifest present, adaptive icons present, privacy policy html exists, Sentry opt-out + sendDefaultPii:false, endpoints list documented in STORE_COMPLIANCE.md.

## Relay hardening goal — 7 defects fixed (2026-07-28, branch main)

**Setup:** repo root `/Users/arshadkazi/Documents/ShiftLogic_HQ/GroceryApp/kimi-k3-variant`; plain checkout (git toplevel = project dir); single branch `main` at `8ad4590`; Node v22.22.3; **no container runtime available** — all Docker-image tests used the documented fallback (scratch dir replicating the Dockerfile COPY set); runtime volume behaviour is UNVERIFIED and remains an owner-side check when deploying. Test baseline: 5 suites / 36 tests / 2.4s.

### Fixes applied (all verified via live HTTP against the running server)
1. **Docker image boot.** Dockerfile now carries an explicit COPY allowlist: `server.js`, `seed-pool.js`, `encrypted-store.js`, `lib/`, `tokens/`, `pool/`, `extract/`; excludes `tokens/__tests__/` and `extract/*.bak*`. `.dockerignore` hardened: `keys/`, `data/`, `relay-state.json`, `used-tokens.json`, `test-relay-state*.json`, `relay-docs.json`, `.env`, test files. Volume mount targets `/data` (RELAY_DATA_DIR) — NOT `/app` — via a pre-created, chowned directory in the image. State files (relay-state.json, used-tokens.json, encrypted-store data/) all respect RELAY_DATA_DIR. docker-compose.yml carries `relay-data:/data` named volume + `RELAY_DATA_DIR=/data`.
2. **Invite forgery closed.** Family membership registry (`familyFounderKeys` Map) records the first enrollment's Ed25519 signing key as the founding member; all subsequent enrollments for that familyId require the signer to be a registered key. Self-sign attack: 403 "Invite signer is not a registered member of this family".
3. **MAX_FAMILIES capacity** counted against `familyDeviceTokens.size` (the enrollment map) instead of the WebSocket-only `familyRooms.size`. A NEW family is rejected only when founding (not when joining an existing one).
4. **Replay set keyed on `${familyId}:${nonce}`** (the canonical signed per-invite nonce), NOT the raw JSON string. Re-serialization (reordered keys, whitespace) yields the same replay key → 403. Nonce is now mandatory (400 if absent); the client always mints one (family.ts:107-108).
5. **Shared `lib/collect-body.js`** module — pre-checks Content-Length, counts bytes while streaming, calls `req.destroy()` on breach. Wired into all 6 body-buffering sites: `/enroll`, `/api/oauth/pair`, `/oauth/token`, `/api/assistant/submit-update`, `/relay/request-token`, `pool/pool-server.js handleContribute`. `extract/extract-server.js` now requires the shared module instead of its own local copy.
6. **`/stats` closed.** Requires `Authorization: Bearer <relayToken>` (same fail-closed mechanism as `/relay/request-token`). Returns aggregate counters only (totalFamilies, totalClients, totalEnrolled, rateLimiters). No `families` map, no device-id prefixes. Wildcard CORS header removed from this response.
7. **Spent-token store permanent.** `UsedTokensStore` stores SHA-256 hashes of tokens (compact 32-byte entropy per record) and NEVER prunes them. `cleanExpired()` is a deliberate no-op — design chosen: hash-based permanent set; reasoning: the token format (`v2.<base64>` = 256-byte RSA sig || 32-byte client-chosen blinded nonce) carries no expiry/timestamp/epoch, so the verifier cannot reject a token for being old; any finite TTL reopens a double-spend window. The alternative (issuer-key epochs) was rejected because it requires client changes and coordination between the issuer and pool.

### Founder-only limitation (documented ceiling, requires client-side handoff)
The relay sees only each joiner's X25519 box public key (`deviceToken`) at enrollment — never the joiner's Ed25519 signing key, and it cannot derive one from the other. Therefore the `familyFounderKeys` registry can only ever hold the **founding device's** signing key. Until the client also sends its Ed25519 signing key at enrollment time, **only the founding device can issue invites this relay will accept**.

**Required client change (next session's first task):** at `GroceryApp/src/identity/enroll.ts:39-54`, extend the POST body to include `signingPublicKey: <base64 Ed25519 pubkey>` (derived at family.ts:111-112 via `crypto_sign_seed_keypair`). The relay would then add that key to `familyFounderKeys` upon successful enrollment. This does NOT weaken zero-knowledge (the Ed25519 key is a public key with no relationship to plaintext list data).

### Deferred to other goals (not fixed here)
- **docker-compose.yml does not provision issuer keypair** in the environment block (lines 23–41). The mount is `./relay-server/keys:/app/keys:ro`, but a fresh deploy with no keys/ directory will fail. Documented in the compose comments; fix collides with the credential-purge goal.
- **server.js:242 seeds fabricated test prices** into the pool on every startup (`seedTestData(poolStore)`). Should be behind a dev-mode gate. Not touched to avoid collision.
- **Turso credential in dist-android `.hbc`**: already addressed by the credential-purge goal (revoked + history rewritten); not revisited.

### New defects found during this work
- **verify-hardening.js state-flush timing:** `persistState()` debounces 500ms; a SIGTERM arriving within 500ms of enrollment loses state unless `gracefulShutdown()` synchronously flushes before closing — it does call `persistState()`, but the implementation is still timeout-based. Not a data-loss risk (the next debounce fires in the handler), but the test needed a 1s sleep before SIGTERM for the CI-level proof.
- No new entries required for LAUNCH-PUNCH-LIST.md (file absent in this working copy; findings recorded here instead).

### Test suite result
Before: 5 suites, 36 tests, 2.4s.
After: **6 suites, 47 tests, ~8s.** (11 new regression tests in `hardening.test.js`.)

## Goal 5 — v1 product surface + recovery-phrase first-run (2026-07-28, branch main)

**Setup:** repo root `/Users/arshadkazi/Documents/ShiftLogic_HQ/GroceryApp/kimi-k3-variant`; single branch `main` (HEAD `7fb69b5`, 1 ahead of origin/main — this copy has no separate launch branch; launch work lives on main after the approved history rewrite). Nine P0 persistence-fix files present and committed (in `7d4148e`). LAUNCH-PUNCH-LIST.md / PRE-LAUNCH-AUDIT.md absent in this copy; worked from the goal brief's inline evidence. App-suite baseline: 40 suites, 476 passed / 1 skipped, tsc clean.

### Decisions
1. **Trip Optimizer gated off (A).** Module-level `const TRIP_OPTIMIZER_ENABLED = false;` with doc comment in `GroceryApp/src/screens/GroceryListScreen.tsx` (direct `{FLAG && (…)}` idiom, same as `MANAGED_TIER_ENABLED`), guarding the single `<StopOptimizer` render site. `<TripPlanSheet` is unreachable transitively — its only parent is StopOptimizer.tsx. No second guard added inside StopOptimizer.tsx (the const is module-local and wouldn't compile from another module). Nothing deleted: stop-optimizer.ts, trip-plan.ts, StopOptimizer.tsx, TripPlanSheet.tsx and stop-optimizer.test.ts all stay for the 1.x flag-flip + entitlement check.
2. **Free-tier posture pinned by test (B).** New `GroceryApp/__tests__/free-tier-posture.test.ts` (source-scanning idiom of turso-gated-off/ac15): proves its guard matcher discriminates against inline guarded/unguarded fixtures before applying it to the real files; asserts all three flags false, the sole render sites, and that "Managed Subscription" stays behind `{!isSelfHosted && (`. Purchase-copy sweep: every hit is BIP39 wordlist, price-normalizer stopwords, React-Navigation/notification `subscribe`, schema-`upgrade`, decrypt-`unlock` copy, or kept managed-tier plumbing. The literal "Managed Subscription"/"Subscription Key"/"Enter your subscription key" strings stay — their enclosing block cannot render while `MANAGED_TIER_ENABLED = false` makes `isSelfHosted` unconditionally true. `ASSISTANT_INTEGRATION` is set nowhere in docker-compose.yml or the relay Dockerfile (relay requires an explicit `=== 'true'`). `isSelfHosted` derivation untouched; ac6-tier-switch stays green.
3. **First-run recovery backup (C).** New persisted flag `recoveryPhraseAcknowledged` (typed in src/types/index.ts, default `false` in src/config/settings.ts). Design: a persistent HomeScreen banner (not a dismissible modal) shown while the flag is unset and a phrase is stored; it navigates to the existing RecoveryScreen `mode:'show'`. The ONLY write of `true` is inside RecoveryScreen's `handleConfirmed` ("I've Stored It Safely"); back/dismiss/backgrounding leave it unset so the banner re-surfaces — the ContributeConsentModal dismiss-suppresses-forever bug is deliberately NOT repeated (that defect itself, in the onCancel/onSkip handlers near SettingsScreen:965/:970, is left for its own fix and is out of this goal's scope). RecoveryScreen show-mode copy made honest: losing phrase + devices = data gone; phrase is superseded if the device later joins another family.
4. **recoverFromPhrase() fix landed (C).** On success it now persists the recovery seed, the normalized phrase, and the stored-flag under the current familyId (skipped when no membership). This makes Settings → View Recovery Phrase work on joining devices, which previously threw "Cannot generate recovery phrase after data has been encrypted." ac13-key-recovery and join-flow stayed green (neither exercises the real function's persistence path; new runtime coverage added in `recovery-first-run.test.ts`). RecoveryScreen's show-mode fallback now only calls generateRecoveryPhrase() when NO master key exists; the phrase-missing-but-keyed state (pre-fix joined devices) raises an actionable error pointing at Recover from Backup.
5. **Persistence errors surfaced (D).** BOTH `persistListToDB(listId).catch(...)` sites in sync-manager.ts (Yjs update observer + remote-update path) now route through a shared `reportPersistError()`: keeps the rejection handled and the console diagnostic, and sets `useSyncStore` `{ syncState: 'error', error: "Couldn't save recent changes to this device" }` (dynamic import — useSyncStore statically imports syncManager, static back-import would cycle). Copy says "save", not "sync": the failure is a local write, and the old blanket "Sync error" label would mislead — SyncIndicator and HomeScreen's inline sync bar now render the store's error message when present, falling back to "Sync error". Covered by `persist-error-surfacing.test.ts` (both sites, runtime, against a rejecting hydrate mock + static catch-site scan).

### New launch-blocking defect found and fixed: first-run provisioning failed on every real device
Documented nowhere in the source documents; found via the Android-emulator device proof for the first-run flow. `react-native-libsodium` exposes **no SHA-256 at runtime** (its JSI layer has no `jsi_crypto_hash_sha256`), so `sodium.crypto_hash_sha256` was `undefined` on device and the BIP39 checksum in `entropyToWordIndices()` threw `TypeError: undefined is not a function` → `generateRecoveryPhrase()` → `provisionFirstRun()` failed → `bootstrapSync()` returned `'no-key'` → **persistence disabled on every real install** (logcat: `[bootstrap] First-run provisioning failed` / `[init] Sync bootstrap: no-key`). The Jest suite stayed green because `__mocks__/react-native-libsodium.js` delegates to `libsodium-wrappers`, which DOES export `crypto_hash_sha256` — the exact "green test against a mock is not evidence" trap. Fix: pure-TS SHA-256 (FIPS 180-4) in `src/identity/recovery.ts`, used at both checksum sites; pinned in `recovery-first-run.test.ts` against libsodium-wrappers' real `crypto_hash_sha256` across 14 input sizes plus NIST vectors, so the mock can never hide the gap again. After the fix, a cleared install on emulator-5554 logs `[init] Sync bootstrap: local-only` and the whole first-run loop was walked on-device: banner → RecoveryScreen → "I've Stored It Safely" → banner gone. Proof screenshots (no phrase captured, kept outside the repo): `/tmp/pantryrun-goal5-proof/02-home-first-run-banner.png`, `/tmp/pantryrun-goal5-proof/03-home-after-acknowledge.png`.

Same-class device gaps found in the audit of `sodium.*` calls vs. react-native-libsodium's real exports, left for the owner / other goals:
- `sodium.crypto_scalarmult_base` in `src/identity/family.ts` (`decryptKeyFromDevice`, near :424) is also undefined on device. Currently latent — `decryptKeyFromDevice` has no runtime caller (only the ac20 test) — but it breaks the moment the sealed-key handoff is wired up. Fix candidates: derive via `crypto_sign_ed25519_pk_to_curve25519` (native ✓) from stored Ed25519 material, or persist the box public key alongside the secret.
- `sodium.crypto_aead_xchacha20poly1305_ietf_ABYTES` is undefined on device (`src/sync/y-websocket.ts` near :433, `src/notifications/NotificationManager.ts` near :254). `slice(0, NaN)`/`slice(NaN)` accidentally produces (empty ciphertext, full cipher||tag) which re-concatenates correctly on decrypt, so it round-trips today by luck — but the stored "ciphertext"/"tag" fields don't mean what they say. Should be replaced with the constant 16 (as crypto/index.ts already does). Not changed here: it touches the sync wire format both relay directions and belongs with a sync-owning goal.

### Test suite result
Before: 40 suites, 476 passed / 1 skipped. After: **43 suites, 503 passed / 1 skipped, 0 failed**; `tsc --noEmit` exit 0. New suites: free-tier-posture (13), recovery-first-run (10, incl. 3 sha256 pins), persist-error-surfacing (4).

### Handoffs / deferred
- **Store-listing conflict (owner → listings goal):** audit-package/07-STORE-LISTINGS.md still advertises Trip Optimizer, which the v1 binary will not have — line 68 (listing bullet "Trip Optimizer: Costco + No Frills saves you $11.40 this week"), line 132 (screenshot 4 of 8: Trip Plan sheet), line 180 (video storyboard 18–24s beat). Not edited here; the listings goal owns that file.
- **Device proof: DONE on Android.** Debug build via `JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home" ANDROID_HOME="$HOME/Library/Android/sdk" npx expo run:android` installed on emulator-5554; after `pm clear`, fresh first run shows the recovery banner, the full acknowledge loop was walked, and the banner cleared. Screenshots (banner + post-acknowledge Home, no phrase captured) at `/tmp/pantryrun-goal5-proof/` — outside the repo. iOS simulator (iPhone 17 Pro, booted) also available if the owner wants an iOS capture: `npx expo run:ios`.
- This goal's changes are left uncommitted on `main` (8 modified files + 3 new test files); the committed persistence-fix set is untouched — no file in the nine-file P0 set was edited by this goal.

## Goal 6 — Compliance, listings, submission prep (2026-07-28, branch main)

**Setup:** repo root `/Users/arshadkazi/Documents/ShiftLogic_HQ/GroceryApp/kimi-k3-variant`; branch `main` (HEAD `7fb69b5`, 1 ahead of origin) — audit-package/ present incl. 00-README.md + 07-STORE-LISTINGS.md, so main carries the launch work. Goals 4/5 work sits UNCOMMITTED on main (20 modified + 3 untracked test files); untouched by this goal except where listed. **Snapshot corrections:** (1) `ls GroceryApp/ios/` shows a FULL native Xcode project (PantryRun/, PantryRun.xcodeproj, Pods/…) with `PantryRun/PantryRun.entitlements` — the snapshot's "JSON-and-Markdown-only" removal assumption is wrong; entitlements/Info.plist must be stripped natively too. (2) The 4-string grep returns 11 matches, not 6: app.json (4), STORE_COMPLIANCE.md (2), PantryRun.entitlements (2), ios/PantryRun/Info.plist (2), src/voice/siri.ts (1, APP_GROUP_ID const). (3) eas.json production NOW HAS an env block: `EXPO_PUBLIC_SENTRY_DSN` = a truthy placeholder (`https://REPLACE_WITH_REAL_DSN@o0.ingest.sentry.io/0`) — sentry.ts (fixed in Goal 4 to read EXPO_PUBLIC_SENTRY_DSN) WOULD initialize against that garbage DSN in a production build; the snapshot's "never initialises" claim no longer holds. (4) `grep -c -i turso GroceryApp/privacy/index.html` → 0, confirmed.

### v1 data-flow truth table (Step 1 — basis for every artifact edit)
Evidence: eas.json has exactly 3 profiles (development, preview, production) + submit.production; only env var is the Sentry placeholder above. Hardcoded-credential re-check: `grep -rn -E "tursoToken|tursoUrl|EXPO_PUBLIC_TURSO" App.tsx src eas.json .env.example` → **no matches (exit 1)**; `initTurso()` has ZERO callers → `isTursoReady()` is permanently false. **Owner answered (2026-07-28, in-session): NO EAS dashboard env vars are configured on any profile.** Owner decisions same session: **Sentry OUT of v1** (remove placeholder env; declare no crash data anywhere) and **barcode lookup gated behind inline consent** (wire `barcodeScanningEnabled`, flyer-scan-style prompt).

| What leaves the device | Where it goes | Ships in v1? | Consent gate | Retention |
|---|---|---|---|---|
| Scanned barcode (digits only) | Turso | **No** — initTurso has no callers; lookupTurso bails at isTursoReady()=false | — | — |
| Scanned barcode (digits only) | world.openfoodfacts.org (Open Food Facts) | **Yes** | `barcodeScanningEnabled` (default false) + first-scan inline consent prompt (added this goal) | Ephemeral request servicing; no account/identifier sent; OFF's own policy governs their logs |
| Scanned barcode | api.nal.usda.gov (USDA) | **No** — requires `EXPO_PUBLIC_USDA_API_KEY`, set nowhere (eas.json, .env.example, dashboard per owner); guard returns null | — | — |
| Postal FSA (3-char) | Turso (Deals) | **No** — Deals tab compiled out (`DEALS_TAB_ENABLED=false`); `loadFsaDeals` bails at isTursoReady()=false before any network | — | — |
| User-typed product names | external price adapters | **No network path** — instacart.ts + scraping.ts are stubs returning null (and unconfigurable: no key path in v1); crowdsourced + flyer-scan adapters are local stores; flipp-deals reads via Turso (dead); registry hashing applies only to these dead externals | — | — |
| Store ID string (e.g. "walmart") | user's own self-hosted relay `/api/pool/prices` | **Yes** (opt-in) | `cloudFlyerEnabled` (default false) | Query only; pool data is aggregate community prices, not user data |
| Flyer photo (EXIF-stripped) | user's own self-hosted relay `/api/extract/flyer` | **Yes** (opt-in) | `flyerScanEnabled` (default true, shows button) AND `pricingOptedIn` (default false, inline consent at tap) | Processed in memory, discarded; NOT E2EE (relay sees image) — disclosed |
| Encrypted CRDT list blobs (XChaCha20-Poly1305 ciphertext) | user's own self-hosted relay | **Yes** | Explicit pairing/enrollment | `UPDATE_TTL_MS` default 2592000000 ms = 30 days (server.js:1685) |
| Device token (generated X25519 public key) + signed invite | user's own self-hosted relay `/enroll` | **Yes** | Explicit pairing | `TOKEN_TTL_MS` default 2592000000 ms = 30 days, sliding on auth (server.js:173) |
| Crash reports | Sentry | **No** — owner decision: placeholder env removed from eas.json; `initSentry()` bails on empty `EXPO_PUBLIC_SENTRY_DSN`; sentry.ts kept for post-v1 | — | — |
| Price contributions (hashed names + prices, blind-token authed) | relay pool | **No** — `submitContributions()` has zero runtime callers (grep: only contribute.ts + tests) | — | — |
| Product name for AI cleanup | `EXPO_PUBLIC_AI_CLEANUP_URL` | **No** — env unset everywhere → heuristic-only local path; also only reachable via submitNewProduct which is Turso-gated (dead) | — | — |
| Voice audio | anywhere | **No** — no mic API in codebase; "voice" input is a text modal; siri.ts has no runtime importers; VOICE_ASSISTANT_LINKING_ENABLED=false, ASSISTANT_INTEGRATION unset | — | — |
| (OS handoff, not collection) store search query incl. FSA | user's maps app via Linking.openURL | user-initiated deep link | explicit tap | n/a |

**Conclusion: nothing in v1 transmits user data to developer-controlled servers or to any third party that retains it.** The only third-party endpoint is Open Food Facts (opt-in barcode lookup, ephemeral, no identifiers). Everything else goes to the user's OWN self-hosted relay (v1 is self-hosted-only; MANAGED_TIER_ENABLED=false verified). Therefore: Apple `NSPrivacyCollectedDataTypes` stays `[]` (deliberately — documented), Apple label = "Data Not Collected", Play Data Safety = "does not collect or share any required user data types", and the old declarations of Crash Data / Device ID / Photos are removed as over-declarations (Sentry is out; device token + flyer photos go only to the user's own server).

### Goal 6 decisions (fix-the-code vs fix-the-promise)
1. **Sentry:** fix-the-code — placeholder `EXPO_PUBLIC_SENTRY_DSN` env REMOVED from eas.json production (a truthy placeholder would have initialized the SDK against a garbage endpoint in every store build). sentry.ts untouched. Re-enabling post-v1 requires: real DSN in eas.json + re-declare Crash Data on both stores + restore policy §6.
2. **Barcode lookup:** fix-the-code — `lookupProduct()` now returns `consent_required` unless `barcodeScanningEnabled`; both scanner entry points (HomeScreen, AddItemSheet) show a flyer-scan-style inline consent prompt on first use writing `barcodeScanningEnabled: true`.
3. **hashItemName():** fix-the-promise — kept FNV-1a but docstring rewritten to say exactly what it is (non-cryptographic, 48-bit, reversible-over-small-vocabulary obfuscation), false "SHA-256" claim deleted; policy no longer claims "one-way hashed" (the external adapters it applied to are dead in v1 anyway).
4. **Relay deletion:** honest-expiry branch + real local wipe. No relay delete route added (self-hosted relay = user's own infra; blobs are ciphertext; 30-day UPDATE_TTL_MS ages them out; destroying local keys makes residue undecryptable). Policy states exactly that.
5. **NSLocalNetworkUsageDescription:** already present in app.json + native Info.plist (Goal 4) — verified, nothing to do.
6. **Siri/App-Group/mic/speech removal** spans app.json AND native ios/PantryRun/PantryRun.entitlements + Info.plist (native project exists). `src/voice/siri.ts` APP_GROUP_ID survives as dead code (no runtime importers) — documented as such.

### Goal 6 — COMPLETED (2026-07-28); what was changed and verified
- **Step 2 — privacy/index.html rewritten** against the truth table: no-collection posture; relay 30-day retention stated (replaces the false "relay stores no persistent data" and "removed automatically on unpair" promises); managed-relay promises removed from policy AND ToS (§4 now says no hosted tier in v1); "one-way hashed" price claim replaced with local-only truth; Voice "(Planned)" → honest no-mic statement; Barcode section now discloses opt-in Open Food Facts lookup (+ inactive USDA fallback); crash-reporting section states nothing is sent; deletion section documents Settings → Delete All Data + undecryptable-residue guarantee. Both "Last updated" lines (privacy L111, ToS L287) = July 28, 2026.
- **Step 3 — declarations reconciled to "nothing collected":** app.json entitlements block (siri + app-groups) and mic/speech purpose strings deleted; same strips in native ios/PantryRun/PantryRun.entitlements and Info.plist (pbxproj has no Siri SystemCapabilities entry — verified). NSPrivacyCollectedDataTypes stays [] and STORE_COMPLIANCE.md §2 now documents it as deliberate ("Data Not Collected"); Play §1 answers "No" to collection with per-flow justification table; old Crash Data / Device ID / Photos declarations removed as over-declarations. Final repo-wide grep for the 4 strings: **zero matches** except `src/voice/siri.ts:25` (dead code, not consumed by any build — nothing imports it at runtime) and prose in docs. Target-SDK line replaced: targets SDK 35 (Expo default, ExpoRootProjectPlugin.kt fallback "35"), current Play floor OK, **SDK 36 required for new apps from Aug 31, 2026** (support.google.com/googleplay/android-developer/answer/11926878, checked 2026-07-28).
- **Step 4 — Delete All Data shipped:** new `src/services/dataWipe.ts` `deleteAllLocalData()` (first reset helper — the unsafeResetDatabase/deleteAllData/resetDatabase grep found none pre-existing): disconnects sync → clears recovery phrase (current family) + passkeys (support flag + current device credential — clearPasskeyData() extended, no longer knowingly incomplete) → destroys master key → membership → relay creds → settings (reuses clearSettings) → device keypair → destroyAllDocs() (new in yjs-adapter) → db.write(unsafeResetDatabase) → zustand store resets. Per-stage error isolation so key destruction can't be blocked. Settings → Danger Zone destructive button with confirmation stating data is UNRECOVERABLE without the recovery phrase. WatermelonDB mock extended with writer-guarded unsafeResetDatabase(). **Pinned by `__tests__/delete-all-data.test.ts` (9 tests)** incl. the honest-limitation case: foreign-family residue survives but master key + device secret are destroyed. Relay branch: honest-expiry (decision 4).
- **In-app copy made true:** PrivacyScreen (crash toggle → "No Analytics or Crash Reporting" info row + Barcode Lookups toggle; deletion bullets → Delete All Data + 30-day expiry; "self-hosted or managed" → self-hosted; stale v1.03 → v1.30), SettingsScreen pricing disclosure + note, GroceryListScreen flyer consent alert (all previously claimed "hashed item names are sent" — nothing is sent in v1). ac14 test updated to pin the new copy.
- **Step 5 — listings reworked:** 07-STORE-LISTINGS.md six-term grep 6→4 (07) and 5→4-ish (06: 5→5 lines but all editorial) — every survivor is editorial prose (rework header, 2.3.1 guardrails, paid-ads roadmap row); zero matches inside description blockquotes/field tables/shot list/storyboard. Trip Optimizer bullet, "find which stores save you the most" promo, Trip Plan shot 4, 18–24s beat, HANDS-FREE blocks (both files), Play's orphaned "minus the Siri line" pointer, deals-feed bullet, "price comparison" short description, and 06's false premise line all gone; 06 Post 3 (trip-optimizer story) rewritten. Extra finds beyond the brief: Apple name count was stale (**30/30**, not 28/30 — predates StopHop→PantryRun rename); keywords carried "price compare/deals/meal" → replaced; "no analytics" bullet KEPT because it is now true. Flyer shots/beats marked photo-library-only while the camera path is broken. All char counts recomputed and shown in both files.
- **Step 6 — owner handoff:** `audit-package/08-SUBMISSION-HANDOFF.md` — per-console paste-ready answers (Apple record/info/pricing/privacy-label/export/review-notes/media slots; Play listing/data-safety/deletion/content declarations/release), capture pipeline (screenshots NOT captured: no signed build + camera bug), batched 9-item owner action list.
- **Gate:** tsc exit 0; app suite **44 suites, 512 passed + 1 skipped** (baseline 43/503+1; new: delete-all-data 9); relay suite **6 suites, 47 passed** (untouched).
- **Not done / stays with owner:** hosting policy + association files (publish = owner confirmation), Team ID, keystore, screenshots/video capture, invite-redirect.html package-id fix (Goal 4 handoff item, sibling file), console form entry + submit.

## Goal 4 — Buildability: iOS identity, relay reachability, signing proof, Sentry, npm ci, eas.json (2026-07-28, branch main)

**Setup:** repo root `/Users/arshadkazi/Documents/ShiftLogic_HQ/GroceryApp/kimi-k3-variant`; single branch `main` (HEAD `7fb69b5`) — no separate launch branch exists in this copy; main carries the launch work (v1.30.0, PantryRun, relay fixes). Goal 5's uncommitted work present (9 modified src files + 3 untracked tests) and untouched. Baseline gate measured before edits: `tsc --noEmit` exit 0; Jest 43 suites, **503 passed** / 1 skipped (above the audited 470). Scratchpad: `/tmp/pantryrun-goal4/` (session-private, wiped between agent sessions — all throwaway artifacts regenerated and evidence captured in-session).

### Environment / toolchain findings (ground truth 7)
- macOS (Darwin). Full **Xcode 26.6 (17F113)** at `/Applications/Xcode.app` — not just CLT. **CocoaPods 1.17.0** present. So local iOS builds are possible on this machine once the owner supplies signing.
- **EAS CLI absent** (`command not found: eas`) — owner install + login (agent must never authenticate).
- `/usr/bin/java` is the macOS stub ("Unable to locate a Java Runtime") — NOT a missing JDK. Real JDK: **OpenJDK 21.0.10 (JBR)** at `/Applications/Android Studio.app/Contents/jbr/Contents/Home` (incl. its own `keytool`). **Android SDK** at `~/Library/Android/sdk` (build-tools 34–37, platforms android-35/36/36.1, apksigner). `JAVA_HOME`/`ANDROID_HOME` were unset — wired per-command; `android/local.properties` written for the build and deleted after (it is gitignored). **No owner install needed for Android builds.**

### Ground-truth corrections (this copy had moved past the 2026-07-27 snapshot)
Already done on main before this session (verified, not redone): `ios.bundleIdentifier com.shiftlogichq.pantryrun` in app.json + full tracked Xcode project in `GroceryApp/ios/` (commit `8f7c525`, incl. ATS exception in Info.plist); `package-lock.json` regenerated so npm ci passes (commit `2532191`); `dist-android/` untracked from git and the stale `android/app/src/main/assets/index.android.bundle` gone (commit `24143d1`); client-side Turso credential paths removed + history rewrite/purge closed out (commits `8cf2927`, `8ad4590`, `f1e5d0c`). Also confirmed per the goal brief: versionCode 30 / versionName 1.30.0 coherent in build.gradle; PANTRYRUN_UPLOAD_* signing wiring present with debug fallback; associatedDomains present; AASA + assetlinks templates present with placeholders. LAUNCH-PUNCH-LIST.md and PRE-LAUNCH-AUDIT.md do NOT exist in this copy.
- **Upload-safety caveat now narrower than the brief:** dist-android/ is untracked from git but still on disk and was NOT in .easignore → added `dist-android` and `*.keystore` to `GroceryApp/.easignore` this session. The tracked source-set bundle is gone, so the "first artifact ships a live token" blocker is CLOSED for Gradle/EAS builds from this tree.
- **Recorded, not fixed (sibling goal, web content):** `GroceryApp/relay-server/public/invite-redirect.html:113` sets androidStoreUrl to Play id `com.groceryapp.app` — wrong package; every Android invite-tap without the app installed lands on a nonexistent listing. Must become `com.shiftlogichq.pantryrun`.
- Stale `com.groceryapp.app` in `GroceryApp/docs/deep-linking-setup.md` (5 places) → fixed to `com.shiftlogichq.pantryrun` this session.

### Decisions
1. **Prebuild fork: option (a) hand-patch, plus full key convergence.** Hand-patched the tracked AndroidManifest.xml (added `android:usesCleartextTraffic="true"` on `<application>`; split the mixed autoVerify filter into https-only autoVerify + separate `groceryapp://invite` filter) AND added `android.versionCode: 30`, `ios.buildNumber: "1"` to app.json so a future prebuild regenerates the same values. Rationale: (b) requires a destructive `--clean` prebuild over hand-edited build.gradle (owner confirmation, higher risk), (c) overlaps the credentials goal and is owner-gated; (a)+explicit-keys removes the "divergence regenerates the defect" objection because app.json now produces the same manifest content. Verified by fresh scratchpad prebuild (iOS) and by expo config (android).
2. **ATS: `NSAllowsLocalNetworking: true`, `NSAllowsArbitraryLoads: false`** in app.json `ios.infoPlist` + `NSLocalNetworkUsageDescription`, mirrored into the tracked `ios/PantryRun/Info.plist` (which already had ATS, lacked the LAN usage string). Did NOT switch the app to wss:// — a LAN self-hosted relay can't practically get a TLS cert; wss-only would delete the primary self-host use case. A remote self-hosted relay with a real domain should still use wss (unchanged behavior).
3. **Sentry: removed `sentry-expo@7.2.0` entirely; single SDK `@sentry/react-native@8.14.0`; plugin `@sentry/react-native/expo` (app.json plugins[0]); import switched in `src/services/sentry.ts`; DSN env renamed to `EXPO_PUBLIC_SENTRY_DSN`** (sentry.ts, .env.example, eas.json env block, STORE_COMPLIANCE.md heading). `enableInExpoDevelopment` dropped (sentry-expo-only option, absent from the v8 API — tsc enforces). Established from installed packages (`app.plugin.js → require('./expo')`) + Sentry docs: sentry-expo is deprecated since Expo SDK 50; @sentry/react-native is the replacement. Inlining proven by sentinel export (hit inside the emitted .hbc).
4. **Lockfile: `package-lock.json` is authoritative; `yarn.lock` deleted (git rm); `"packageManager": "npm@10.9.8"` pinned in package.json.** CI and eas both run npm; yarn.lock was divergent and unowned. Lockfile regenerated after the sentry-expo removal; clean-dir `npm ci` re-verified after (added 1043 packages, exit 0).
5. **eas.json production:** `cli.appVersionSource: "local"` (deliberate: remote would move version ownership onto EAS servers, contradicting the explicit android.versionCode 30 in git, and needs an authenticated project), `autoIncrement: false`, android `buildType: "app-bundle"`, env `EXPO_PUBLIC_SENTRY_DSN` with a REPLACE_WITH_REAL_DSN placeholder, ios `resourceClass: m-medium`.
6. **Signing proven with a throwaway keystore** (`/tmp/pantryrun-goal4/throwaway-signing-test.keystore`, generated with the JBR keytool, passed via `-PPANTRYRUN_UPLOAD_*` CLI props only): `:app:bundleRelease` BUILD SUCCESSFUL in 6m7s; `keytool -printcert -jarfile app-release.aab` → `CN=Throwaway Signing Test, OU=Goal4, O=PantryRun` (NOT CN=Android Debug). Keystore deleted after; `git status --short` clean of new artifacts. **The throwaway is NOT the upload key; the real upload keystore is an owner deliverable the agent never generates or holds.** Residual (unchanged, by design in build.gradle): with no `-P` props, assembleRelease/bundleRelease silently debug-signs — acceptable for local wiring tests, but the owner must always pass the real props for store artifacts.
7. **iOS prebuild check** run in `/tmp/pantryrun-goal4/ios-prebuild-copy` (APFS clone, ios/ deleted first for fresh generation; `TMPDIR` overridden — sandbox blocks `/var/folders`): generated pbxproj `PRODUCT_BUNDLE_IDENTIFIER = "com.shiftlogichq.pantryrun"`, generated Info.plist has the ATS dict, the LAN usage string, and CFBundleVersion 1. Working-copy `ios/` untouched except the intended 2-line Info.plist edit. NOTE: this copy tracks the full ios/ project (bare-ish workflow); whether to keep it tracked vs. going CNG is an open owner decision.
8. **Gate after all changes:** tsc exit 0; Jest 43 suites, 503 passed / 1 skipped — equal to the measured baseline. (One transient failure of a latency-sensitive test occurred while a background npm ci saturated the machine; not reproducible in two consecutive clean runs.)

### CI note
`.github/workflows/ci.yml` triggers on push to main and PRs to main; since launch work IS main in this copy, CI runs on every push. With the regenerated lockfile npm ci passes, so the merge-time-failure claim in the older docs is obsolete here.

### Owner handoff (batched — also in the final session report)
1. **Apple Team ID** → replace `TEAMID` in `GroceryApp/ios/apple-app-site-association` before hosting; needed for any iOS signing. Read it from Apple Developer → Membership.
2. **iOS signing**: certificates + provisioning via Xcode or `eas credentials` after `eas login` (owner-only; agent never authenticates). EAS CLI is not installed: `npm install -g eas-cli`.
3. **Android upload keystore** (generate, back up OFF-repo; losing it without Play App Signing = locked out of updates; `*.keystore` is NOT gitignored — never move it into the repo):
   `keytool -genkeypair -v -keystore pantryrun-upload.keystore -alias pantryrun-upload -keyalg RSA -keysize 2048 -validity 10000`
   (use `/Applications/Android Studio.app/Contents/jbr/Contents/Home/bin/keytool`). Then build store artifacts with `-PPANTRYRUN_UPLOAD_STORE_FILE=<abs path> -PPANTRYRUN_UPLOAD_STORE_PASSWORD=… -PPANTRYRUN_UPLOAD_KEY_ALIAS=pantryrun-upload -PPANTRYRUN_UPLOAD_KEY_PASSWORD=…`.
4. **assetlinks.json SHA-256**: enrol in Play App Signing (recommended) and use the fingerprint from Play Console → Setup → App integrity → App signing key certificate (NOT the upload key's), replacing `REPLACE_WITH_SHA256_FINGERPRINT_OF_YOUR_SIGNING_CERT`.
5. **Host both association files** at `https://groceryapp.app/.well-known/` per `GroceryApp/docs/DEEP-LINK-HOSTING.md` (application/json, HTTPS, no redirects, iOS file extensionless with `_comment` stripped).
6. **Sentry DSN**: create the Sentry project, put the real DSN into eas.json's `EXPO_PUBLIC_SENTRY_DSN` (replacing REPLACE_WITH_REAL_DSN) or a local `.env`.
7. **Fix `GroceryApp/relay-server/public/invite-redirect.html:113`** Play package → `com.shiftlogichq.pantryrun` (sibling goal owns the file).
8. **Decisions to make now (cheap only until first submission):** display name (PantryRun vs CartNest/SnugCart/KinCart — affects expo.name/listing only) and whether groceryapp.app stays the invite domain. **The bundle/application id `com.shiftlogichq.pantryrun` becomes permanently immutable the moment the App Store Connect record is created — last cheap moment to object is before that.**
9. Environment wiring the owner does NOT need to install: JDK (Android Studio JBR) and Android SDK are present; only EAS CLI (and nothing else) is missing for cloud builds.

## Goal 7 — PantryRun Plus (paid tier) — gate WAIVED by owner, build in progress (2026-07-28, branch main)

**GATE: WAIVED.** Owner first confirmed in chat (2026-07-28): **"v1 hasn't been submitted to either store yet."** Asked again same day with explicit options; owner selected: **"Not submitted — waive gate, build now"** — i.e. v1 is still not submitted, the owner explicitly waives the post-launch gate and instructs the Plus build to proceed now, accepting that the paid-tier work lands before v1's store review outcome is known. Recorded verbatim; Goal 7 work begins on that instruction.

### Baseline measured this session (pre-gate, read-only)
- Root `/Users/arshadkazi/Documents/ShiftLogic_HQ/GroceryApp/kimi-k3-variant`; single branch `main`, HEAD `7fb69b5`, 1 ahead of origin/main. Working tree carries Goals 4–6 uncommitted (32 modified, 1 deleted yarn.lock, 7 untracked) — untouched.
- Gate: `npx tsc --noEmit` exit 0; Jest **44 suites, 512 passed / 1 skipped**. Scripts: `test` = jest; no typecheck script (use `npx tsc --noEmit`).
- `TRIP_OPTIMIZER_ENABLED = false` exists (module-local const, GroceryListScreen.tsx:72; sole render site :1030 gating `<StopOptimizer>`). No IAP SDK installed (package.json + lockfile grepped for react-native-iap / react-native-purchases: zero). No `trip-plan.test.ts`; `stop-optimizer.test.ts` exists. MONETIZATION.md + audit-package/05 present; LAUNCH-PUNCH-LIST.md / PRE-LAUNCH-AUDIT.md absent (not sought elsewhere).
- **Storage position re-verified in this copy** (matches the goal brief): schema.ts `version: 5` with grocery_lists / grocery_items / family_members / notifications / offline_queue; migrations.ts highest `toVersion: 5`; hydrate.ts persists/loads only items, lists, members; **no entitlement table or persistence path**; nothing in yjs-adapter.ts / sync-manager.ts persists encoded Yjs doc state. So the family-unlock model needs a **v5→v6 entitlements table migration** (offline_queue pattern) to survive restart.
- **Savings divergence re-verified at all four sites:** trip-plan.ts :239–:258 (worst-single-store synthetic baseline, `Math.max(0, savings)`); stop-optimizer.ts :130 `savingsVsOneStop = bestOneStopCost - bestCombinedCost`; TripPlanSheet.tsx :6 comment "savings vs. most expensive single store" + :213 bare "💰 You save" label. Still unresolved; owner choice pending when the goal starts.

### Owner decisions captured 2026-07-28 (to re-confirm price/model only if stale when the gate opens)
1. **Entitlement model = option (2), family unlock:** one purchase writes an entitlement record into the family Yjs doc (E2EE sync) backed by a new local `entitlements` table; receipt validation at purchase time. Owner chose it knowing it is forgeable by modified clients (accepted stance — public repo) and that the local table is what makes it survive restart.
2. **Price = $14.99/year** (audit-package recommendation, AnyList household-plan anchor). Owner approved explicitly.
3. **IAP SDK = react-native-iap** (no RevenueCat; keeps purchase data between app and stores, no third-party account, no extra privacy-label entry).
4. **Savings semantics = reconciled to the one-stop baseline** (owner decision 2026-07-28, option "Reconcile to one-stop baseline"): trip-plan.ts `savings` is now best-single-store-trip cost − optimized total, floored at 0 — the same comparison stop-optimizer.ts reports as `savingsVsOneStop`. The old synthetic worst-case basket (per-item max across stores) is gone. TripPlanSheet.tsx header comment corrected; label stays "💰 You save" per the approved option. The number is now smaller but honest and matches the store-listing one-stop framing.
5. **New defect found and fixed (owner approved fix in-goal, 2026-07-28):** computeTripPlan compared candidate store subsets purely on totalCost while unassigned items cost 0, so a subset covering FEWER items could win and silently drop items whenever store catalogs were partial (the normal real-world case). Found by the new direct test suite — a 1-item specialist store beat six stores covering everything. Fix: coverage-first comparison (`isBetterPlan`: fewer unassigned wins, cost breaks ties) applied to the exact path, the greedy seed, and greedy additions (greedy now also adds stores that increase coverage at higher cost). One-stop baseline store selected the same way so a 1-item store can't masquerade as the one-stop alternative. Regression-pinned in trip-plan.test.ts ("DEFECT FIXED" case).
6. **Direct trip-plan tests added** (`GroceryApp/__tests__/trip-plan.test.ts`, 20 tests): all eight required cases — empty items/stores zeroed shape, zero relevant stores, unpriced→unassigned with price 0, maxStops clamping, exact(≤7) vs greedy(>7) with an 8-store case asserting greedy ≤ best single store, savings floor at 0, quantity into totalCost and subtotals. Suite after: **45 suites, 532 passed / 1 skipped; tsc clean** (baseline 44/512+1).

### Resume checklist — superseded by the waiver above; work proceeded this session in the brief's order.

### Goal 7 build — what landed (2026-07-28, all uncommitted on main)
- **Storage:** schema v6 (`entitlements` table: family_id, product_id, platform, transaction_id, purchased_at, expires_at nullable, source, updated_at); migrations `toVersion: 6` createTable (offline_queue pattern); `EntitlementModel` registered in database.ts. Numbered against the verified v5 reality.
- **Entitlement module:** `GroceryApp/src/config/entitlements.ts` — the ONLY file computing entitlement state (module-header invariant + pinned by tests). Zustand store `useEntitlementStore`; `resolveEntitlement()` (App.tsx init, after bootstrapSync) loads the local table and installs a family-doc observer; `purchasePlus()`/`restorePlus()` use react-native-iap v15 (Nitro, event-based: requestPurchase + purchaseUpdated/ErrorListener; type 'subs'; finishTransaction) with a purchase-time receipt-presence check (JWS/purchaseToken) before granting; `grantEntitlement()` persists locally (single flat db.write — nested-write deadlock trap respected), writes the record into the family Yjs doc's top-level `entitlements` map, and registers `__family__` with syncManager so the grant propagates (list docs were registered per-list; the family doc was NOT previously registered for outbound sync — without this the entitlement would never reach other devices). Cross-device path: remote Y.applyUpdate fires the map observer → persist with source 'sync' → entitled. Server-side receipt validation (App Store Server API / Play Developer API) deliberately absent — owner-credential-gated and out of the accepted honest-user threat model.
- **Gating:** GroceryListScreen `TRIP_OPTIMIZER_ENABLED = true`; sole render site now `{TRIP_OPTIMIZER_ENABLED && isPlus && (<StopOptimizer…)}`; un-entitled users see an upsell row → paywall Alert (copy states the one-stop savings baseline precisely) with Subscribe/Restore. SettingsScreen gained a "PantryRun Plus" section (subscribe/restore/active states); MANAGED_TIER_ENABLED stays false — the dead key-entry field does NOT ship next to the IAP flow.
- **IAP SDK:** react-native-iap **15.6.2** (+ react-native-nitro-modules 0.36.3 peer, in lockfile). No config plugin exists or is needed (autolinked; Play BILLING permission comes from the library manifest). New `src/types/ambient.d.ts` declares `global` (iap ships raw TS sources that tsc type-checks).
- **Tests:** `trip-plan.test.ts` (20 — the 8 required cases + regression for the coverage-blindness fix), `entitlements.test.ts` (6 — fresh-install not-entitled + guard short-circuit, grant survives simulated relaunch, expired/foreign-SKU/receipt-less records refused, family-doc sync entitles device B and survives its relaunch), `free-tier-posture.test.ts` rewritten to the Plus posture (flag true, flag+isPlus guard shape, entitlement single-source invariants: react-native-iap imported only by entitlements.ts, store created once, no other file touches the entitlements table). **Gate: 46 suites, 543 passed / 1 skipped, tsc clean** (session baseline 44/512+1).
- **Version bump:** 1.30.0 → **1.31.0**, versionCode 30 → **31** (app.json + package.json + android/app/build.gradle + ios Info.plist CFBundleShortVersionString; CFBundleVersion stays 1 for the new marketing version) — new native dep ⇒ new build/new review, planned as a release.
- **Docs:** MONETIZATION.md + 05-PREMIUM grandfathering guidance corrected (cohort is empty — feature was gated off pre-v1; no exemption logic built); 05 status line now "gated out of v1 … ships now with PantryRun Plus"; savings-led Trip Optimizer assets restored in 06 (claimable list, listing don'ts, description block, Post 3) and 07 (header, PLUS description block + Apple auto-renew metadata note, guardrails, screenshot-plan row #4, 18–24s storyboard beat) — all copy states the honest one-stop baseline.
- **Environment (checked, not assumed):** full Xcode at /Applications/Xcode.app; `/usr/bin/java` is the macOS stub but the JBR JDK + Android SDK wiring from Goal 4 works (local.properties written for the debug-build proof, gitignored); **EAS CLI not installed** — owner installs + logs in.
- **Known limitation (recorded, deliberate):** `expiresAt` is stored null — the stores own renewal; a lapsed subscription keeps working locally until a restore/re-resolve against the store. Tightening this needs server-side receipt validation (owner credentials) or on-device StoreKit2/Billing entitlement queries at launch — candidate for the next release; out of scope per "do not over-engineer the gating".

### Goal 7 device proof (2026-07-28; both platforms compile with react-native-iap linked)
**iOS:** `pod install` locks `NitroIap` (from ../node_modules/react-native-iap) + `NitroModules 0.36.3`; `xcodebuild -workspace PantryRun.xcworkspace -scheme PantryRun -sdk iphonesimulator CODE_SIGNING_ALLOWED=NO build` → **BUILD SUCCEEDED** (4m37s). Tracked-file side effects: Podfile.lock (NitroIap entries) + a 2-line pbxproj addition (openiap-versions.json in [CP] Copy Pods Resources) — both legitimate pod-install output for the tracked native project; Pods/ and build/ are gitignored. STORE_COMPLIANCE.md §7 v1 note corrected (it claimed "no in-app purchase path"); IAP console answers added as 08-SUBMISSION-HANDOFF.md §5.
**Android (emulator-5554 Pixel_8_Pro, debug build):**
`:app:assembleDebug` BUILD SUCCESSFUL in 3m4s with the IAP native module compiled in; merged manifest carries `com.android.vending.BILLING`. Fresh install (pm clear): logcat shows `[init] Sync bootstrap: local-only` then **`[entitlements] resolved: not entitled (no local record)`**; the list screen renders the locked upsell row ("🔒 Trip Optimizer — unlock with PantryRun Plus ($14.99/year)") and NO StopOptimizer; tapping it shows the paywall alert with the honest one-stop copy + Subscribe/Restore/Not Now. Screenshots (outside the repo): `/tmp/pantryrun-goal7-proof/01-fresh-install-home.png`, `03-list-screen.png`, `04-paywall-alert.png`. local.properties written for the build and deleted after; no build artifacts tracked.

### Goal 7 purchase-case status (done_when §80)
- Fresh install / not entitled: **EVIDENCED** (device log + screenshots above; also entitlements.test.ts case 1).
- (a) purchase → force-quit → relaunch: **BLOCKED** — needs the `pantryrun_plus_annual` products to exist in App Store Connect + Play Console AND a sandbox tester Apple ID / Play license tester (owner-only; agent must not create accounts). Runtime-equivalent restart-survival is pinned by entitlements.test.ts ("grant survives a simulated app restart").
- (b) device A → device B, same family, different store account: **BLOCKED** on the same prerequisites; the CRDT propagation path (family-doc entitlements map → observer → local persist → entitled, surviving B's relaunch) is pinned by entitlements.test.ts.
- (c) uninstall → reinstall → recovery-phrase restore: **BLOCKED** on the same prerequisites. Verified the user-reachable restore flow EXISTS (Settings → "🔑 Recover from Backup" → RecoveryScreen mode 'recover', landed in Goal 5) — the case is meaningful. Note: recovery restores the family key/data; the store purchase itself is re-attached via Restore Purchase (same store account) or the family-doc sync once any family device is online.
- iOS StoreKit local test config: evaluated — a `.storekit` configuration in the Xcode scheme could exercise the iOS purchase UI without ASC products; useful as a pre-sandbox smoke test but NOT a substitute for a real sandbox run (per the goal brief). Owner can request it once product IDs are final.

### Goal 7 — OWNER HANDOFF (batched; clear in one sitting) — status 2026-07-28: owner DEFERRED all three asks in-session (release strategy "Defer"; products/testers "Not yet — will do later"; EAS builds "Not yet"). Purchase cases and build-ID recording stay blocked on items 2–4 below; everything agent-side is finished.
1. **Release-strategy decision (new, created by the gate waiver):** v1 was never submitted, and Plus now sits on the same branch at v1.31.0. Either (A) submit the free v1.30 first (needs a separate checkout/tag before these changes) and ship 1.31 as the post-launch update the plan assumed, or (B) make 1.31.0-with-Plus the initial store submission (Apple: the IAP products must be attached to the first version's review). Decide and record here.
2. **Create the subscription products (both consoles; agent must not):** Product ID **`pantryrun_plus_annual`** in both. Apple: Auto-Renewable Subscription, new group "PantryRun Plus", reference name "PantryRun Plus Annual", display name "PantryRun Plus", description "Unlocks the Trip Optimizer for your whole family: the cheapest way to split your list across nearby stores, with savings shown vs. the cheapest single store.", duration 1 year, price **$14.99 USD/year** (pick matching CAD tier), localization en-US (+ en-CA). Play: Subscription `pantryrun_plus_annual`, base plan **`annual`** (auto-renewing, 1 year, $14.99), which must be **ACTIVE**. **The base plan ID must match `PLUS_BASE_PLAN_ID` in `GroceryApp/src/config/entitlements.ts` exactly** — the earlier claim here that "the client passes an empty offerToken and relies on the single base plan" was false and shipped as a bug: Play Billing v5+ rejects a purchase whose offer has no token, so the client now fetches the token at runtime and matches on `basePlanId`. Any other spelling of the base plan ID fails 100% of Android purchases. See audit-package/08-SUBMISSION-HANDOFF.md §Play for the full note. Owner approves final price at creation.
3. **Testers (agent must not create):** one sandbox tester Apple ID (App Store Connect → Users and Access → Sandbox) and one Play license tester (Play Console → Settings → License testing). Then report back so the three blocked purchase cases can be run and evidenced.
4. **Build:** `npm install -g eas-cli && eas login` (owner credentials), then from `GroceryApp/`: **`eas build --platform all --profile production`**. Expected output: one iOS + one Android build ID/URL — paste them back and they get recorded here. Local sanity available without EAS: Android `:app:bundleRelease` with the `-PPANTRYRUN_UPLOAD_*` props (Goal 4 handoff item 3); iOS via Xcode once signing exists.
5. **Receipt-validation credentials (specify, never paste values in chat):** when server-side validation is wanted (next release), generate an App Store Server API key (ASC → Integrations, .p8 stays in the owner's secret store) and a Play Developer API service account (Cloud console, JSON key in the owner's secret store). NOT needed for this release — the shipped model validates receipt presence at purchase time by deliberate decision.
6. **Store paperwork for the IAP:** Apple privacy label — recommend keeping **`NSPrivacyCollectedDataTypes: []` / "Data Not Collected"** (verified still `[]` in app.json): purchases run through StoreKit/Play directly, the entitlement record syncs E2EE through the user's own relay, and no purchase data reaches developer-controlled servers; document that reasoning in the review notes. Play: answer "yes" to "does your app include in-app purchases" (monetization declaration; Data safety stays "no data collected"). Re-answer Apple's "paid digital content" question as yes — products attached to the build.
7. **Marketing capture additions:** screenshot-plan row #4 (Trip Plan sheet on a Plus-entitled build) and the 18–24s storyboard beat need a device with the entitlement granted — available after item 3 via a sandbox purchase, or immediately via a debug build with a row seeded in the entitlements table.
8. **Invite-redirect.html fixed (2026-07-28, resolves handoff item 7):** `relay-server/public/invite-redirect.html` had 5 bugs: (a) Play package ID `com.groceryapp.app` → `com.shiftlogichq.pantryrun` (the actual package); (b) app scheme typo `grocceryapp://` → `groceryapp://` (matching app.json `"scheme": "groceryapp"`); (c) title/h1 "GroceryApp" → "PantryRun"; (d) iOS App Store URL cleaned (removed stale `groceryapp` slug, kept `idXXXXXXXXXX` placeholder with comment to replace once ASC record exists); (e) status message "Opening GroceryApp…" → "Opening PantryRun…". Screenshot capture attempted via iOS simulator (iPhone 17 Pro Max, xcodebuild product from prior turn) — Metro bundler would not stay running in the background (suspended TTY output; `curl localhost:8081/status` timed out); the app installed but crashed on launch without the JS bundle. Screenshots remain an owner-side deliverable (need a production build via EAS or a working Metro session).
9. **Browser-driven store submission capability (2026-07-28, verified):** The user asked whether the agent can drive the browser to handle the full store-submission process. The browser-use MCP server is confirmed operational — navigated to `https://appstoreconnect.apple.com` (redirected to ASC login at `/login`; screenshot at `GroceryApp/test_screenshots/appstore-connect-login.png`) and `https://play.google.com/console` (lands on the about/landing page; screenshot at `GroceryApp/test_screenshots/play-console-login.png`). **Both consoles require owner authentication before any console work can begin.** The ASC login form is served from a cross-origin iframe (`idmsa.apple.com`), so the agent cannot and should not interact with it — this is the owner-only authentication step. Once authenticated, the ASC dashboard pages are same-origin (`appstoreconnect.apple.com`) and fully interactive via the browser tools (navigate, click, fill, upload_file, take_screenshot, take_snapshot). The Play Console developer dashboard is similarly same-origin once authenticated. **Agent capability confirmed:** every form field in 08-SUBMISSION-HANDOFF.md (store listings, privacy questionnaires, IAP product creation, review notes, content ratings, data safety, data deletion) can be filled via the browser once the owner is logged in. **Agent cannot:** authenticate (owner credentials + 2FA), accept legal agreements, set up banking/tax, run EAS builds, capture device screenshots, or press the final Submit button. **App configuration verified correct for store submission:** app.json version 1.31.0 / versionCode 31, bundleIdentifier + package `com.shiftlogichq.pantryrun`, `ITSAppUsesNonExemptEncryption: true`, `NSPrivacyCollectedDataTypes: []`, `NSPrivacyTracking: false`, associated domains configured, permissions set correctly; eas.json production profile with `app-bundle` for Android + `m-medium` iOS; entitlements.ts product ID `pantryrun_plus_annual` with lazy IAP import and single-place gating invariant. **Flag:** `supportsTablet: true` in app.json means iPad 13" screenshots (2064×2752) are REQUIRED for Apple submission — owner must either capture these or flip `supportsTablet` to false before building (08-SUBMISSION-HANDOFF.md §1.7).
